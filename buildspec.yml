version: 0.1

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  files:
    - 'dist/**/*'
    - 'package.json'
    - 'gulpfile.js'
    - '.env'
    - '.sequelizerc'